### domain_group

| 컬럼명 | 타입 | 제약조건 | 컬럼설명 |
| --- | --- | --- | --- |
| id | BIGINT | PK, 
GENERATED BY DEFAULT AS IDENTITY | 고유-그룹 번호, 자동증가 |
| leader_id | BIGINT | FK → domain_user.Id,  
NOT NULL,
ON DELETE CASCADE | 그룹장의 고유-유저번호 |
| created_at | TIMESTAMP | NOT NULL,  
DEFAULT CURRENT_TIMESTAMP | 그룹 생성 시점 |
| limit_member_count | SMALL INT | NOT NULL,
DEFAULT 50 | 최대 수용 그룹원 수 |
| image | TEXT |  | 그룹 공식 이미지 경로 |
| name | VARCHAR(100) | NOT NULL | 그룸의 이름 |
| description | TEXT | NOT NULL | 그룹 설명 |
| is_private | BOOLEAN | NOT NULL, 
DEFAULT FALSE | 그룹 공개 여부 |
| auto_assign | BOOLEAN | NOT NULL, 
DEFAULT FALSE | 가입신청 자동 승인 여부 |
| invitation_code | VARCHAR(50) | UNIQUE | 초대코드 |
    

### group_tag

| 컬럼명 | 타입 | 제약조건 | 설명 |
| --- | --- | --- | --- |
| id | BIGINT | PK, GENERATED BY DEFAULT AS IDENTITY |  |
| group_id | BIGINT | FK → domain_group.id, 
NOT NULL,
ON DELETE CASCADE |  |
| tag | VARCHAR(50) | NOT NULL |  |
| UNIQUE(group_id, tag) |  |  |  |

### group_problem

| 컬럼명 | 타입 | 제약조건 | 컬럼설명 |
| --- | --- | --- | --- |
| id | BIGINT | PK, GENERATED BY DEFAULT AS IDENTITY | 고유-그룹문제번호, 자동증가 |
| problem_id | BIGINT | FK → problem_summary.Id,
NOT NULL,
ON DELETE CASCADE | 원본 문제의 고유-문제번호 |
| group_id | BIGINT | FK → domain_group.Id,
NOT NULL,
ON DELETE CASCADE | 그룹문제가 속하는 그룹의 고유-그룹번호 |
| assigned_at | TIMESTAMP | NOT NULL | 그룹문제가 해당 그룹에 추가된 시점 |
|  |  |  |  |
| **관련 컬럼** | **제약조건명** | **제약조건 로직** | **이유** |
| **group_id, 
problem_id** | UK_GROUP_PROBLEM_UNIQUE | UNIQUE (group_id, problem_id) | [중복 방지] 한 그룹 내에 동일한 문제가 두 번 이상 등록되는 것을 원천 차단함. |

### group_post

| 컬럼명 | 타입 | 제약조건 | 설명 |
| --- | --- | --- | --- |
| id | BIGINT | PK, GENERATED BY DEFAULT AS IDENTITY | 고유-GroupPost번호, 자동증가 |
| group_id | BIGINT | FK → domain_group.id, NOT NULL,
ON DELETE CASCADE | GroupPost가 속한 Group의 고유번호. |
| author_id | BIGINT | FK → domain_user.id, NOT NULL,
ON DELETE CASCADE | GroupPost를 작성한 고유-유저번호 |
| title | VARCHAR(200) | NOT NULL | GroupPost의 제목 |
| content | TEXT | NOT NULL | GroupPost의 본문 |
| is_notice | BOOLEAN | NOT NULL, DEFAULT FALSE | 공지사항으로 설정 여부 |
| view_count | INT | NOT NULL, DEFAULT 0 | GroupPost가 확인된 횟수 (조회수) |
| created_at | TIMESTAMP | NOT NULL, DEFAULT CURRENT_TIMESTAMP | GroupPost가 생성된 시점. |
| updated_at | TIMESTAMP | NOT NULL, DEFAULT CURRENT_TIMESTAMP | GroupPost가 마지막으로 수정된 시점 |

### group_post_comment

| 컬럼명 | 타입 | 제약조건 | 설명 |
| --- | --- | --- | --- |
| id | BIGINT | PK, GENERATED BY DEFAULT AS IDENTITY |  |
| post_id | BIGINT | FK → group_post.id, 
NOT NULL,
ON DELETE CASCADE |  |
| author_id | BIGINT | FK → domain_user.id, 
NOT NULL,
ON DELETE CASCADE |  |
| content | TEXT | NOT NULL |  |
| created_at | TIMESTAMP | NOT NULL, DEFAULT CURRENT_TIMESTAMP |  |
| updated_at | TIMESTAMP | NOT NULL, DEFAULT CURRENT_TIMESTAMP |  |

### group_user

| 컬럼명 | 타입 | 제약조건 | 설명 |
| --- | --- | --- | --- |
| id | BIGINT | PK, GENERATED BY DEFAULT AS IDENTITY |  |
| group_id | BIGINT | FK → domain_group.id, 
NOT NULL,
ON DELETE CASCADE |  |
| user_id | BIGINT | FK → domain_user.id, 
NOT NULL,
ON DELETE CASCADE |  |
| role | VARCHAR(10) | NOT NULL | `LEADER` / `MEMBER` |
| joined_at | TIMESTAMP | NOT NULL | 승인 시점 |
| UNIQUE(group_id, user_id) |  |  |  |

### group_assign_request

| 컬럼명 | 타입 | 제약조건 | 설명 |
| --- | --- | --- | --- |
| id | BIGINT | PK, GENERATED BY DEFAULT AS IDENTITY |  |
| group_id | BIGINT | FK → domain_group.id, NOT NULL |  |
| user_id | BIGINT | FK → domain_user.id, NOT NULL |  |
| status | VARCHAR(10) | NOT NULL | PENDING/ APPROVED/ REJECTED/ CANCELED |
| applied_at | TIMESTAMP | NOT NULL,
DEFAULT CURRENT_TIMESTAMP | 신청 시점 |
| decided_at     | TIMESTAMP | NULL | 결정시점 |
| decided_by | BIGINT | FK → domain_user.id, NULL | 승인/거절한 관리자 user_id |
| decided_reason | VARCHAR(255) | NULL | 결정 사유 |